# Spark Kubernetes Quickstart

## Requirements

- Openshift origin (`oc cluster up`)
- `oc create -f http://radanalytics.io/resources.yaml` (in alternative: `oc create -f ./oshinko-resources.yaml`)
- `oc new-app oshinko-webui`

